sed -i 's/val next = (currentEwma \* 0.8 + obs.latencyMs \* 0.2).toLong()\n                    }\n                    synchronized(recentLatencies) {\n                        recentLatencies\[latencyIndex\] = obs.latencyMs\n                        latencyIndex = (latencyIndex + 1) % 100\n                        if (latencyCount < 100) latencyCount++\n                    ewmaLatencyMs.set(next)/val next = (currentEwma \* 0.8 + obs.latencyMs \* 0.2).toLong()\n                    ewmaLatencyMs.set(next)\n                }\n                synchronized(recentLatencies) {\n                    recentLatencies[latencyIndex] = obs.latencyMs\n                    latencyIndex = (latencyIndex + 1) % 100\n                    if (latencyCount < 100) latencyCount++\n                }/' app/src/main/java/com/aistudio/pinkproxy/fresh/StrategyState.kt
