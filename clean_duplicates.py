import re

def clean_when_blocks(content):
    # This is a bit tricky, let's just do simple regex replacements for specific known blocks.
    # Actually, a safer way is to find the exact line numbers from gradle output and remove those blocks.
    return content

