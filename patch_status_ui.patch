--- app/src/main/java/com/aistudio/pinkproxy/fresh/MainActivity.kt
+++ app/src/main/java/com/aistudio/pinkproxy/fresh/MainActivity.kt
@@ -300,7 +300,7 @@
                             isProbing -> "AUTOPILOT PROBING STRATEGIES..."
                             isStalled -> "TRAFFIC STALL DETECTED"
                             !isProxyHealthy -> stringResource(R.string.status_recovery)
-                            else -> stringResource(R.string.status_autopilot)
+                            else -> if (isActive && isProxyHealthy && !isStalled) stringResource(R.string.status_autopilot) + " + " + stringResource(R.string.status_dns_prefetch) else stringResource(R.string.status_autopilot)
                         },
                         fontSize = 11.sp,
                         fontWeight = FontWeight.Bold,
