sed -i '/buffer.add(DatagramPacket(payload.copyOfRange(offset, offset + length), length, targetInet, targetPort))/a \                        scope.launch { delay(25); val toSend = synchronized(buffer) { val c = buffer.toList(); buffer.clear(); c }; toSend.forEach { try { socket.send(it) } catch(e: Throwable){} } }' app/src/main/java/com/aistudio/pinkproxy/fresh/UdpTransportHandler.kt
