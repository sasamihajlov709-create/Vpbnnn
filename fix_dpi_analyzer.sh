sed -i 's/val tcpStates = StrategyStateRepository.getAllContextStates().filterKeys { it.transport == TransportType.TCP && it.profileId == currentProfileId }.values/val tcpStates = StrategyStateRepository.getStates(profileId = currentProfileId, transport = TransportType.TCP)/' app/src/main/java/com/aistudio/pinkproxy/fresh/DpiAnalyzer.kt
sed -i 's/val udpStates = StrategyStateRepository.getAllContextStates().filterKeys { it.transport == TransportType.UDP && it.profileId == currentProfileId }.values/val udpStates = StrategyStateRepository.getStates(profileId = currentProfileId, transport = TransportType.UDP)/' app/src/main/java/com/aistudio/pinkproxy/fresh/DpiAnalyzer.kt
sed -i 's/val dnsStates = StrategyStateRepository.getAllContextStates().filterKeys { it.transport == TransportType.DNS && it.profileId == currentProfileId }.values/val dnsStates = StrategyStateRepository.getStates(profileId = currentProfileId, transport = TransportType.DNS)/' app/src/main/java/com/aistudio/pinkproxy/fresh/DpiAnalyzer.kt
sed -i 's/val states = StrategyStateRepository.getAllContextStates().filterKeys { it.profileId == currentProfileId }.values/val states = StrategyStateRepository.getStates(profileId = currentProfileId)/' app/src/main/java/com/aistudio/pinkproxy/fresh/DpiAnalyzer.kt
sed -i 's/val states = StrategyStateRepository.getAllContextStates().filterKeys { it.transport == transport && it.profileId == currentProfileId }.values/val states = StrategyStateRepository.getStates(profileId = currentProfileId, transport = transport)/' app/src/main/java/com/aistudio/pinkproxy/fresh/DpiAnalyzer.kt
