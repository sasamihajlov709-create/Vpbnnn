--- app/src/main/java/com/aistudio/pinkproxy/fresh/PinkProxyServer.kt
+++ app/src/main/java/com/aistudio/pinkproxy/fresh/PinkProxyServer.kt
@@ -74,6 +74,10 @@
         }
     }
 
+    fun forceRecovery(reason: String) {
+        _proxyHealthTrigger.tryEmit(reason)
+    }
+
     fun logRecovery(msg: String) {
         Log.w("PinkProxyServer", msg)
     }
