sed -i 's/val buffer = if (isStreaming) BufferPool.obtain() else BufferPoolManager.obtain16k()/val buffer = if (isStreaming) BufferPool.obtain() else BufferPoolManager.obtain16k()\n                    var totalBytesRead = 0L\n                    val streamStartTime = System.currentTimeMillis()/' app/src/main/java/com/aistudio/pinkproxy/fresh/TcpTransportHandler.kt

sed -i 's/if (read > 0 && !recordedFullTransfer.getAndSet(true)) {/totalBytesRead += read\n                            if (!recordedFullTransfer.get() \&\& (totalBytesRead > 32768 || (System.currentTimeMillis() - streamStartTime > 2000 \&\& totalBytesRead > 0))) {\n                                if (!recordedFullTransfer.getAndSet(true)) {/' app/src/main/java/com/aistudio/pinkproxy/fresh/TcpTransportHandler.kt
