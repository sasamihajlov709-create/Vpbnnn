--- app/src/main/java/com/aistudio/pinkproxy/fresh/MainActivity.kt
+++ app/src/main/java/com/aistudio/pinkproxy/fresh/MainActivity.kt
@@ -974,7 +974,10 @@
                             Row(modifier = Modifier.fillMaxWidth().padding(vertical = 2.dp), horizontalArrangement = Arrangement.SpaceBetween) {
                                 Text(text = host, color = Color(0xFFF8BBD0), fontSize = 10.sp, maxLines = 1, modifier = Modifier.weight(1f))
-                                Text(text = count.toString(), color = Color(0xFFF48FB1), fontSize = 10.sp, fontWeight = FontWeight.Bold)
+                                val strategy = BypassConfig.resolveSessionConfigForHost(host).strategy.name
+                                Text(text = "$count - $strategy", color = Color(0xFFF48FB1), fontSize = 10.sp, fontWeight = FontWeight.Bold)
                             }
                         }
